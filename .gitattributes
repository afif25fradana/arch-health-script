# Set default behavior for all files to prevent issues with line endings.
* text=auto eol=lf

# Ensure shell scripts always use LF line endings, as they are run on Linux.
*.sh text eol=lf

# Ensure Windows batch files (if any) use CRLF line endings.
*.bat text eol=crlf
*.cmd text eol=crlf
